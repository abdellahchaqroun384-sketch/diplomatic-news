(() => {
  const stories = window.STORIES || [];
  const nav = [['MAROC','maroc'],['BELGIQUE','belgique'],['DIASPORA','diaspora'],['DIPLOMATIE','diplomatie'],['INTERNATIONAL','international'],['EUROPE','europe'],['SOCIÉTÉ','societe'],['ENQUÊTES','enquetes'],['ANALYSES','analyses'],['OPINION','opinion']];
  const ticker = ["Ceuta : Sánchez reste isolé dans sa défense du Maroc · Euractiv, 7 sept. 2026","La migration revient au premier plan de l'agenda européen · Euronews, 9 sept. 2026","Kaja Kallas participe au Sommet européen de l'Arctique · EEAS, 14 sept. 2026","Le Comité militaire de l'UE renforce sa coopération avec l'Agence européenne de défense · EEAS, 9 sept. 2026","L'UE et le Groenland renforcent leur partenariat avec 200 millions d'euros · EEAS, 7 sept. 2026"];
  const logo = '<img src="assets/diplomatic-news-logo.webp" alt="Diplomatic News">';
  const href = path => `#/${path}`;
  const meta = s => `<div class="story-meta"><span>${s.date}</span><span class="meta-dot">·</span><span>◷ ${s.read} min</span></div>`;
  const card = s => `<article class="news-card"><a href="${href('article/'+s.slug)}"><div class="card-image"><img src="${s.image}" alt="${s.title}"><span>${s.category}</span></div><div class="card-content">${meta(s)}<h3>${s.title}</h3><p>${s.excerpt}</p><span class="card-arrow">↗</span></div></a></article>`;
  function shell(content, active='') { return `<div class="site-shell"><div class="topline"><span>ÉDITION INTERNATIONALE · 15 SEPTEMBRE 2026</span><span class="topline-links"><a href="${href('a-propos')}">À propos</a><span>·</span><span>Rabat · Bruxelles · Monde</span></span></div><header class="site-header"><div class="header-main container"><button class="mobile-menu-button" id="menuBtn" aria-label="Ouvrir le menu">☰</button><a class="brand" href="#/">${logo}</a><div class="header-actions"><button class="header-control language-switch" id="langBtn" aria-label="Passer à l'arabe"><span class="control-icon">ع</span><span class="control-label">العربية</span></button><button class="header-control" id="themeBtn" aria-label="Changer le thème"><span class="control-icon">☾</span><span class="control-label">Mode</span></button><a class="header-control" href="${href('recherche')}" aria-label="Rechercher"><span class="control-icon">⌕</span><span class="control-label">Recherche</span></a></div></div><nav class="main-nav"><div class="container nav-inner"><a class="${active===''?'active-nav':''}" href="#/">À LA UNE</a>${nav.map(([n,p])=>`<a class="${active===p?'active-nav':''}" href="${href(p)}">${n}</a>`).join('')}</div></nav></header><div class="ticker"><div class="ticker-viewport"><div class="ticker-track">${[...ticker,...ticker].map((t,i)=>`<span class="ticker-item" key="${i}"><b>À LA UNE</b>${t}<i>✦</i></span>`).join('')}</div></div></div>${content}<footer class="site-footer"><div class="container footer-grid"><div class="footer-brand"><a class="footer-wordmark" href="#/">${logo}</a><p>La vérité avant tout.</p><small>Un journal indépendant depuis Rabat, Bruxelles et au-delà.</small></div><div><span class="footer-label">Navigation</span><a href="#/">À la une</a><a href="${href('recherche')}">Recherche</a><a href="${href('a-propos')}">À propos</a><a href="${href('auteurs')}">Auteurs</a></div><div><span class="footer-label">Rubriques</span><a href="${href('maroc')}">Maroc</a><a href="${href('diplomatie')}">Diplomatie</a><a href="${href('international')}">International</a><a href="${href('enquetes')}">Enquêtes</a></div><div><span class="footer-label">Informations</span><a href="${href('contact')}">Contact</a><a href="${href('mentions-legales')}">Mentions légales</a><button id="footerLang">Français / العربية</button></div></div><div class="container footer-bottom"><span>© 2026 Diplomatic News</span><span>Indépendance · Transparence · Pluralisme</span></div></footer><div class="drawer-backdrop" id="drawer"><aside class="mobile-drawer"><div class="drawer-head"><span class="eyebrow">NAVIGATION</span><button id="closeDrawer">×</button></div><a class="drawer-home" href="#/">À la une ↗</a>${nav.map(([n,p])=>`<a href="${href(p)}">${n} ↗</a>`).join('')}</aside></div></div>`; }
  function home() { const lead=stories[0]; return shell(`<main class="container multi-page-main"><section class="hero-copy multi-hero"><div><span class="eyebrow"><span class="gold-rule"></span> ÉDITION DU JOUR</span><p class="hero-intro">L'actualité diplomatique et internationale décryptée avec exigence, depuis Rabat, Bruxelles et au-delà.</p><div class="hero-stat"><strong>54</strong><span>articles, 10 rubriques,<br>un regard indépendant.</span></div></div><h1>La vérité<br>avant tout<span class="gold-dot">.</span></h1></section><section class="hero-grid"><a href="${href('article/'+lead.slug)}" class="lead-story"><div class="image-wrap"><img src="${lead.image}" alt="${lead.title}"><span class="image-label">À LA UNE · ${lead.category}</span></div><div class="lead-body">${meta(lead)}<h2>${lead.title}</h2><p>${lead.excerpt}</p><span class="read-more">Lire l'article ↗</span></div></a><div class="side-stories">${stories.slice(1,4).map(s=>`<a class="side-story" href="${href('article/'+s.slug)}"><img src="${s.image}" alt="${s.title}"><div><span class="story-category">${s.category}</span><h3>${s.title}</h3>${meta(s)}</div></a>`).join('')}</div></section><section class="latest"><div class="section-heading"><div><span class="eyebrow">LE FIL ÉDITORIAL</span><h2>Dernières actualités</h2></div><a class="text-link" href="${href('recherche')}">Rechercher ↗</a></div><div class="latest-grid">${stories.slice(4,16).map(card).join('')}</div></section><section class="editorial-band"><div class="band-intro"><span class="eyebrow">NOS CHOIX ÉDITORIAUX</span><h2>Enquêtes</h2><p>Les histoires qui demandent du temps, des sources et du courage.</p><a class="outline-button" href="${href('enquetes')}">Toutes les enquêtes ↗</a></div><a class="investigation-feature" href="${href('article/'+stories[18].slug)}"><img src="${stories[18].image}" alt="${stories[18].title}"><div class="investigation-overlay"><span class="story-category">ENQUÊTE</span><h3>${stories[18].title}</h3>${meta(stories[18])}</div></a><div class="band-list">${stories.slice(26,29).map(s=>`<a class="band-list-item" href="${href('article/'+s.slug)}"><span>${s.category}</span><h3>${s.title}</h3>${meta(s)}</a>`).join('')}</div></section><section class="newsletter"><div><span class="eyebrow">LA NOTE DIPLOMATIC</span><h2>Une lecture claire du monde, chaque semaine.</h2><p>Recevez notre sélection éditoriale et nos analyses, sans bruit.</p></div><form class="email-input" id="newsletter"><span>✉</span><input required type="email" placeholder="Votre adresse email"><button>S'inscrire</button></form></section></main>`); }
  function category(section) { const list=stories.filter(s=>s.section===section); return shell(`<main class="container category-page"><div class="category-heading"><div><span class="eyebrow">DOSSIER ÉDITORIAL · ${list.length} ARTICLES</span><h1>${section}</h1><p>Retrouvez toute l'actualité, les analyses et les décryptages de la rubrique ${section.toLowerCase()}.</p></div><span>↗</span></div><div class="subcategory-nav"><span>Explorer la rubrique</span><button>Actualités</button><button>Analyses</button><button>Décryptage</button></div><div class="latest-grid category-grid">${list.map(card).join('')}</div></main>`, section.toLowerCase().replace('é','e')); }
  function article(slug) { const s=stories.find(x=>x.slug===slug)||stories[0]; const related=stories.filter(x=>x.section===s.section&&x.id!==s.id).slice(0,3); return shell(`<main class="article-page container"><div class="breadcrumbs"><a href="#/">À la une</a><span>/</span><strong>${s.category}</strong></div><a class="back-link" href="#/">← Retour à la une</a><div class="article-heading"><span class="story-category">${s.category}</span><h1>${s.title}</h1><p>${s.excerpt}</p><div class="article-byline"><span class="author-chip"><span class="author-avatar">${s.author[0]}</span><span><b>${s.author}</b><small>Correspondant éditorial</small></span></span>${meta(s)}</div><div class="share-row"><span>Partager</span><button>f</button><button>𝕏</button><button>in</button><button id="copyLink">↗</button></div></div><img class="article-hero" src="${s.image}" alt="${s.title}"><div class="article-layout"><article class="article-body"><p class="article-lead">${s.excerpt}</p><h3>Ce qu'il faut retenir</h3><ul><li>Une séquence suivie avec attention par les acteurs régionaux.</li><li>Des décisions qui s'inscrivent dans un temps long.</li><li>Un enjeu de transparence, de dialogue et de mise en œuvre.</li></ul>${s.body.split('\n\n').map(p=>`<p>${p}</p>`).join('')}<h3>Les lignes de fond</h3><p>Au-delà des déclarations, ce dossier révèle les transformations silencieuses qui redessinent les relations entre institutions, territoires et citoyens.</p><blockquote>« La vérité avant tout : comprendre les faits pour mieux lire le monde. »</blockquote></article><aside class="article-sidebar"><div class="sticky-side"><div class="side-newsletter"><span class="eyebrow">LA NOTE DIPLOMATIC</span><h3>Le monde, sans le bruit.</h3><p>Une sélection éditoriale chaque semaine.</p><form><input type="email" placeholder="Votre email"><button>S'inscrire</button></form></div><span class="eyebrow">À LIRE AUSSI</span>${related.map(x=>`<a class="related-link" href="${href('article/'+x.slug)}"><span>${x.category}</span>${x.title}</a>`).join('')}</div></aside></div></main>`, s.section.toLowerCase().replace('é','e')); }
  function search() { return shell(`<main class="container search-page"><span class="eyebrow">RECHERCHE ÉDITORIALE</span><h1>Trouver un article</h1><div class="search-box page-search"><span>⌕</span><input id="searchInput" autofocus placeholder="Maroc, diplomatie, Bruxelles..."><button id="clearSearch">×</button></div><div id="searchResults"><p class="search-hint">Saisissez au moins deux caractères pour rechercher dans les 54 articles.</p></div></main>`); }
  function info(type) { const isAbout=type==='a-propos'; return shell(`<main class="container info-page"><span class="eyebrow">${isAbout?'NOTRE MANIFESTE':'LA RÉDACTION'}</span><h1>${isAbout?'Comprendre le monde':'Les signatures'}<br><em>${isAbout?'sans raccourcis.':'derrière les histoires.'}</em></h1><p class="info-lead">${isAbout?'Diplomatic News est un média indépendant qui regarde le Maroc, la Belgique et le monde depuis les deux rives de la Méditerranée.':'Une équipe éditoriale distribuée entre Rabat, Bruxelles et les grands terrains d’actualité.'}</p>${isAbout?'<div class="commitment-grid"><div><span>01</span><h2>Indépendance</h2><p>Nos choix éditoriaux ne dépendent d’aucun agenda politique ou commercial.</p></div><div><span>02</span><h2>Transparence</h2><p>Nous expliquons nos méthodes, nos sources et nos corrections.</p></div><div><span>03</span><h2>Pluralisme</h2><p>Nous faisons place aux faits, aux voix et aux points de vue qui se répondent.</p></div></div>':`<div class="authors-grid">${[...new Set(stories.map(s=>s.author))].map((a,i)=>`<div class="author-card"><span class="author-avatar large">${a[0]}</span><div><span class="eyebrow">${String(i+1).padStart(2,'0')}</span><h2>${a}</h2><p>Reportage, vérification et décryptage.</p></div></div>`).join('')}</div>`}</main>`, isAbout?'':'auteurs'); }
  function render() { const raw=location.hash.replace(/^#\/?/,''); const parts=raw.split('/').filter(Boolean); let view=parts[0]==='article'?article(parts[1]):parts[0]==='recherche'?search():parts[0]==='a-propos'?info('a-propos'):parts[0]==='auteurs'?info('auteurs'):parts[0]?category(parts[0].toUpperCase()==='SOCIETE'?'SOCIÉTÉ':parts[0].toUpperCase()==='ENQUETES'?'ENQUÊTES':parts[0].toUpperCase()==='ANALYSES'?'ANALYSES':parts[0].toUpperCase()):home(); document.getElementById('app').innerHTML=view; window.scrollTo(0,0); bind(); }
  function bind(){ document.getElementById('themeBtn')?.addEventListener('click',()=>{document.documentElement.classList.toggle('dark');localStorage.setItem('theme',document.documentElement.classList.contains('dark')?'dark':'light');}); document.getElementById('langBtn')?.addEventListener('click',toggleLang); document.getElementById('footerLang')?.addEventListener('click',toggleLang); const drawer=document.getElementById('drawer');document.getElementById('menuBtn')?.addEventListener('click',()=>drawer.classList.add('open'));document.getElementById('closeDrawer')?.addEventListener('click',()=>drawer.classList.remove('open'));drawer?.addEventListener('click',e=>{if(e.target===drawer)drawer.classList.remove('open')}); document.getElementById('newsletter')?.addEventListener('submit',e=>{e.preventDefault();e.currentTarget.innerHTML='<strong>Inscription confirmée.</strong> La prochaine Note Diplomatic est pour vous.'}); document.getElementById('copyLink')?.addEventListener('click',()=>navigator.clipboard?.writeText(location.href)); const input=document.getElementById('searchInput'); const results=document.getElementById('searchResults'); input?.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();if(q.length<2){results.innerHTML='<p class="search-hint">Saisissez au moins deux caractères pour rechercher dans les 54 articles.</p>';return}const found=stories.filter(s=>`${s.title} ${s.excerpt} ${s.category} ${s.author}`.toLowerCase().includes(q));results.innerHTML=found.length?`<p class="results-count">${found.length} résultat(s) pour « ${input.value} »</p><div class="latest-grid category-grid">${found.map(card).join('')}</div>`:'<div class="empty-search"><h2>Aucun résultat trouvé</h2><p>Essayez un autre mot-clé ou explorez une rubrique.</p></div>'}); document.getElementById('clearSearch')?.addEventListener('click',()=>{input.value='';input.dispatchEvent(new Event('input'))}); }
  function toggleLang(){ const rtl=document.documentElement.dir==='rtl';document.documentElement.dir=rtl?'ltr':'rtl';document.documentElement.lang=rtl?'fr':'ar';document.body.classList.toggle('rtl',!rtl); }
  if(localStorage.getItem('theme')==='dark')document.documentElement.classList.add('dark'); window.addEventListener('hashchange',render); render();
})();
